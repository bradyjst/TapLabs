export type TapLabsTheme = {
  visualizerOverlay: string;
  // base surfaces
  bg: string;
  surface: string;
  border: string;
  grid: string;

  // text
  textPrimary: string;
  textMuted: string;

  // accents
  accent: string;
  accentSoft: string;
  accentBorder: string;

  // feedback colors
  perfect: string;
  early: string;
  late: string;

  // gameplay visuals
  noteColor: string;
  approachColor: string;
  backgroundImage?: string;
};